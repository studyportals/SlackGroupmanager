# SlackGroupmanager

https://api.slack.com/custom-integrations/legacy-tokens to create token to add as a BEARER environment variable

use `DEBUG` env var with `SlackGroup:*` as value for debugging and run with `npm run dev` to test.
